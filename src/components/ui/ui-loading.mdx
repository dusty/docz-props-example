---
name: UiLoading
route: '/ui-spinner'
---

import { Playground } from 'docz'
import { UiLoading } from './ui-loading'

# UiLoading

Renders the default application spinner. Can be used inside any components that need to present a spinner.

## Example

<Playground>
  <UiLoading />
</Playground>
